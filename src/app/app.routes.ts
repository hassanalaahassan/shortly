import { Routes } from '@angular/router';
import { ShortyyComponent } from '../layouts/shortyy/shortyy.component';

export const routes: Routes = [
  {
    path:'',
    component:ShortyyComponent,
  }
];
